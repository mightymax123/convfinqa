"""
Dummy file added to ensure when this is PR'd in the pipeline passes.
"""


def add(a: int, b: int) -> int:
    """
    Adds two integers.

    Args:
        a (int): First integer.
        b (int): Second integer.

    Returns:
        int: Sum of the two integers.
    """
    return a + b
